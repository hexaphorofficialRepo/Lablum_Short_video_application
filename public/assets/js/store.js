const rootEl=document.documentElement;const themeMode=localStorage.getItem("theme");const sidebar=document.querySelector(".sidebar-wrapper");if(themeMode==="dark"){rootEl.classList.add("dark");rootEl.classList.remove("light");rootEl.classList.remove("semiDark")}else if(themeMode==="semiDark"){rootEl.classList.add("semiDark");rootEl.classList.add("light");rootEl.classList.remove("dark")}else{rootEl.classList.add(themeMode);rootEl.classList.remove("dark");rootEl.classList.remove("semiDark")}
if(localStorage.effect==="grayScale"){rootEl.classList.add("grayScale")}else{rootEl.classList.remove("grayScale")}
if(localStorage.dir==="rtl"){rootEl.dir="rtl"}else{rootEl.dir="ltr"}
if(localStorage.menuLayout=="horizontalMenu"){rootEl.classList.add("horizontalMenu")}else{rootEl.classList.remove("horizontalMenu")}
if(localStorage.contentLayout=="layout-boxed"){rootEl.classList.add("layout-boxed")}
if(localStorage.navbar=="floating"){rootEl.classList.add("nav-floating");rootEl.classList.remove('nav-sticky');rootEl.classList.remove('nav-hidden');rootEl.classList.remove('nav-static')}else if(localStorage.navbar=="sticky"){rootEl.classList.add("nav-sticky");rootEl.classList.remove('nav-floating');rootEl.classList.remove('nav-hidden');rootEl.classList.remove('nav-static')}else if(localStorage.navbar=="hidden"){rootEl.classList.add("nav-hidden");rootEl.classList.remove('nav-floating');rootEl.classList.remove('nav-sticky');rootEl.classList.remove('nav-static')}else{rootEl.classList.add("nav-static");rootEl.classList.remove('nav-floating');rootEl.classList.remove('nav-sticky');rootEl.classList.remove('nav-hidden')}